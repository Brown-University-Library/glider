

// This is the global object that the user interacts with

export function getGliderAPI({gliderApp, parts, phases, places}) {
  return {
    parts: parts,
    phases: phases,
    places: places
  }
}

